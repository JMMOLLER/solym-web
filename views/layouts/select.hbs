<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
    <title>Document</title>
</head>
<body>
    <style>
        .card {
            width: calc(50% - 2em);
            margin: 1em;
            width: 18rem; 
            min-height: 15cm;
            max-height: 22cm
        }
        .container {
            margin: 0 auto;
        }
        .options{
            width: 100%;
            display: inline-flex;
        }
        button{
            margin: 20px auto ;
        }
        .text-container {
            min-height: 150px;
        }
        label, input {
            margin-top: 10px;
        }
        .card-title{
            min-height: 70px;
        }
    </style>
    <h1>PLEASE SELECT YOUR MUSIC</h1>
    {{#if error}}
        <h1>No se encontraron coincidencias con tu archivo de música</h1>
    {{/if}}
    <div class="container">
		
	    <div class="card-columns" style="display:flex;flex-wrap:wrap;">
            
                {{#each songs}}
                    <div class="card">
                        <img src="{{this.result.header_image_url}}" class="card-img-top" alt="image" style="max-height: 300px; min-height: 300px;">
                        <div class="card-body">
                            <h5 class="card-title">{{this.result.full_title}}</h5>
                            <div class="text-container">
                                <p class="card-text">title: {{this.result.title}}</p>
                                <p class="card-text">Full title: {{this.result.full_title}}</p>
                            </div>
                            <input type="checkbox" class="btn-check" id="{{this.result.id}}" autocomplete="off" data-target="{{this.result.id}}">
                            <label class="btn btn-primary" for="{{this.result.id}}">Single toggle</label>
                        </div>
                    </div>
                {{/each}}
        </div>
        <div class="options">
            <button type="button" class="btn btn-warning" id="previous">Regresar</button>
            <button type="button" class="btn btn-success" id="next">Continuar</button>
        </div>
    </div>


    <script>
        let selected;

        const checkboxes = document.querySelectorAll('input[type="checkbox"]');

        checkboxes.forEach((checkbox) => {
            checkbox.addEventListener('change', () => {
                if(checkbox.checked) {
                    selected = checkbox.dataset.target;
                    console.log(selected);
                }
            });
        });

        const next = document.getElementById('next');
        const previous = document.getElementById('previous');
        next.addEventListener('click', () => {
            if(selected){
                document.cookie = `selectedTrack={id:${selected}}`;
                window.location.href = `/start`;
            }else{
                alert('Please select a track');
            }
        });
        previous.addEventListener('click', async() => {
            await fetch('/api/delete', {
                method: 'DELETE',
            }).then((res) => {
                window.location.href = `/`;
            });
            
        });
    </script>
</body>
</html>